# Disable gateway JWT verification for invite-to-board; function validates user internally.
# Fixes 401 when Supabase gateway has issues with ES256/session tokens.
[functions.invite-to-board]
verify_jwt = false
